eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone:
        - "http://localhost:${server.port}/eureka"

server:
  port: 8761

management:
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  profiles:
    group:
      lab2:
        security:
    active: lab2
