hazelcast:
  cluster-name: dev
  instance-name: animal-service
  network:
    join:
      kubernetes:
        enabled: true
        service-name: hazelcast
        service-port: 5701
      multicast:
        enabled: true
