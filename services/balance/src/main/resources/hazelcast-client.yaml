hazelcast:
  cluster-name: dev
  instance-name: balance-service
  network:
    join:
      kubernetes:
        enabled: true
        service-name: hazelcast
        service-port: 5701
#      multicast:
#        enabled: true
